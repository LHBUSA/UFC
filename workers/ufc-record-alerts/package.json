{
  "name": "ufc-record-alerts",
  "private": true,
  "type": "module",
  "scripts": {
    "build:check": "wrangler deploy --dry-run --outdir=.wrangler-dryrun",
    "test": "node --test src/recordAlerts.test.mjs"
  }
}
